{
  "installCommand": "corepack enable && corepack prepare pnpm@9 --activate && pnpm config set registry https://registry.npmmirror.com && pnpm install --no-frozen-lockfile --prod=false || true",
  "buildCommand": "npx tsc && vite build"
}